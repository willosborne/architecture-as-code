apiVersion: v1
kind: Service
metadata:
  name: application
spec:
  selector:
    app: application
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
